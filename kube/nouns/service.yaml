apiVersion: v1
kind: Service
metadata:
  name: nouns-service
  namespace: app
spec:
  selector: 
    app: nouns
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
      nodePort: 30081  # Exposes the service externally on this port

  type: LoadBalancer
